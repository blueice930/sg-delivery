import React from 'react';
import { Button } from '@material-ui/core';

const Shop = () => {
  const abc = 123;
  return (
    <div>
      Shop page
      <Button
        variant="contained"
        color="primary"
      >
        test
      </Button>
    </div>
  );
};

export default Shop;
